@model IEnumerable<Category>

<div class="list-group">

        <a href="/"class="list-gro-item list-group-item">Tüm Kategoriler</a>

    @foreach (var i in Model)
    {

        <a  asp-controller="Home" asp-action="Index" asp-route-id="@i.Id"
        class="list-gro-item list-group-item 
        @(ViewBag.SelectedCategory==i.Id.ToString()? "active":"")">
        @i.Name
        </a>
    }

</div>
